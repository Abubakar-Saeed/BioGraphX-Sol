# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# Source research notebook and scratch extraction (superseded by the
# organized modules in this repo)
*.ipynb
.ipynb_checkpoints/
_cells/
.claude/

# Manuscript sources - reference material only, not part of this repo
*.tex
*.bib
BioGraphX_Sol.tex
Supplementary.tex

# ESM-2 embedding caches - regenerate with esm_embeddings.py (large, and
# tied to whatever raw sequences you're currently working with).
esm_embeddings/
*.npz

# Ad-hoc / regenerable training run output. "Encoded Data/" and
# "Models Weights/" ARE tracked in this repo (unlike BioGraphX-IDP) -
# only new runs outside those two folders are ignored.
biographx_sol_nn_results*/
models_protsatt/
importance_protsatt/
*.zip

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
